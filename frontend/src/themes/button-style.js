import { createMuiTheme } from "@material-ui/core/styles";

const buttonTheme = createMuiTheme({
  palette: {
    button: { backgroundColor: "#61892F", color: "#FFFFFF" },
  },
});

export default buttonTheme;
