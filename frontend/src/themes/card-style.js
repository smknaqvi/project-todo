import { createMuiTheme } from "@material-ui/core/styles";

const cardTheme = createMuiTheme({
  palette: {
    card: { backgroundColor: "#474B4F", color: "#FFFFFF" },
  },
});

export default cardTheme;
