import { connect } from "react-redux";
import HHTriviaQuestion from "../components/hh-trivia-question.component";

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(HHTriviaQuestion);
