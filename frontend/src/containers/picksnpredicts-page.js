import { connect } from "react-redux";
import PicksnPredictsPage from "../components/picksnpredicts-page.component";

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(PicksnPredictsPage);
