import { connect } from "react-redux";
import TriviaQuestion from "../components/trivia-question.component";

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({});

export default connect(mapStateToProps, mapDispatchToProps)(TriviaQuestion);
